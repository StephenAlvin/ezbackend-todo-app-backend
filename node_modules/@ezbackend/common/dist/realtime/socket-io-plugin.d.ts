import { Server } from "socket.io";
import { EzBackendInstance } from '..';
declare module 'fastify' {
    interface FastifyInstance {
        io: Server;
    }
}
export declare const createSocketIO: (instance: EzBackendInstance, opts: any) => Promise<void>;
export declare const attachSocketIO: (instance: EzBackendInstance, opts: any) => Promise<void>;
