import { EzApp } from '@ezbackend/common';
/**
 * Use this for building route prefixes.
 * Pass in the instance and plugin prefix to generate a proper route prefix.
 * @param instancePrefix
 * @param pluginPrefix
 * @returns
 */
export declare function buildRoutePrefix(instancePrefix: string, pluginPrefix: string): string;
export declare class EzDbUI extends EzApp {
    constructor();
}
